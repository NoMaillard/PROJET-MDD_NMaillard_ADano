import Level

def isAllowed(level):
    if :
        return False
    else :
        return True


def spawn(level,state):
    if state == True:
        while isAllowed(level) == False:
            x = 